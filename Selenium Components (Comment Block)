"""
Selenium Components

1. WebDriver
   - WebDriver is the core component of Selenium.
   - It communicates directly with the browser using browser-specific drivers
     like ChromeDriver.
   - It sends commands such as click, type, and navigate to the browser.

2. Selenium Grid
   - Selenium Grid allows tests to run on multiple machines, browsers,
     and operating systems simultaneously.
   - It helps reduce execution time through parallel testing.

3. Selenium IDE
   - Selenium IDE is a browser extension.
   - It is mainly used for recording and playing back test cases.
   - It can also generate automation scripts in different programming languages.
"""
